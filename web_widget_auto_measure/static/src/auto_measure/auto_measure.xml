<templates>
    <t t-name="web_widget_auto_measure.AutoMeasureWidget" owl="1">
       <div class="o_auto_measure_widget" t-if="showWidget">
            <span>
                <!-- Mostramos el valor flotante (no reactivo es el valor que enía el campo al abrirse) -->
                <!-- <t t-esc="value"/> -  -->
                <!-- Mostramos el valor flotante (no reactivo ? no tendria que ser el this.state.amount?) -->
                <!-- <span t-if="props.readonly" t-esc="formattedValue" /> -  -->
                <!-- <span  t-esc="formattedValue" /> -  -->
                <t t-esc="amount"/> 
            </span>
           <i t-att-class="'fa fa-circle ' + (circleColor === 'red' ? 'text-danger' : 'text-success')"
           style="margin-left: 10px;"></i>
        </div>
    </t>
</templates>


